// functions for the AUT encoder
// function prototypes for encoder.cpp
#include "mbed.h"

// initialise the encoders
void InitEncoder(void);

// get and reset the left encoder count
int LeftEncoder(void);

// get and reset the left encoder count
int RightEncoder(void);
